# Installation Instructions

# Install packages
sudo pacman -S plank alacritty firefox vlc discord

# Copying dotfiles
cd ~/pixarch/dock
sudo cp -r *.desktop /usr/share/applications
sudo cp dock.theme /usr/share/plank/themes/Transparent
